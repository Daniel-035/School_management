# Base URL for backend API requests. Vite exposes VITE_* values to the browser,
# so this file must never contain passwords, service accounts, or private keys.
VITE_API_BASE_URL=http://localhost:8080/api
